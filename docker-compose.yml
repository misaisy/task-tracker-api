services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8000}:8000"
    environment:
      - APP_HOST=0.0.0.0
    env_file:
      - .env

  test:
    build: .
    command: pytest
    env_file:
      - .env